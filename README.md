# String_input

```
input string in c with dynamic memory (unlimite string input)

run with makefile
    -> in win and linux 
      -> make
      -> main
      
   output : (input string and enter)
            (ouput same string)


run without makefile
    -> 	gcc -c uinput.c -o uinput.o
	      gcc -c main.c -o main.o
	      gcc uinput.o main.o -o main
      
   output : (input string and enter)
            (ouput same string)


```
